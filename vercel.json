{
  "buildCommand": "npm run lint && npm run typecheck && NODE_ENV=test npm test && npm run build",
  "crons": [
    {
      "path": "/api/push/notify",
      "schedule": "0 8 * * *"
    },
    {
      "path": "/api/revalidate-today",
      "schedule": "10 0 * * *"
    }
  ],
  "git": {
    "deploymentEnabled": false
  }
}
